#template config zebra

hostname {{ hostnamesrv }}
password quagga
enable password quagga
log file /var/log/quagga/zebra.log

interface lo
ip address {{ router_id }}/{{ router_id_mask }}

#interface {{ ifname }}
{% for if in hostname.ifname %}
  interface {{ if }}
{% endfor %}

ip forwarding
